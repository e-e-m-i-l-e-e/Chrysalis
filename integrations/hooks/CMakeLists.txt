add_library(HooksLibrary INTERFACE)
target_include_directories(HooksLibrary INTERFACE include)
if(WIN32)
    find_package(polyhook2 REQUIRED)
    target_link_libraries(HooksLibrary INTERFACE polyhook2::polyhook2)
endif()